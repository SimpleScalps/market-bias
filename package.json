{
  "name": "market-bias",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Finanznachrichten mit automatischer Bullish/Bearish-Einordnung",
  "scripts": {
    "fetch": "node scripts/fetch.mjs",
    "test": "node --test src/sentiment.test.mjs",
    "serve": "node --run fetch && npx --yes http-server docs -p 8080 -c-1"
  }
}
